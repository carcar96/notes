#### 1. 基本的基本状态
——>创建状态、就绪状态、执行状态、阻塞状态、终止状态
#### 2. 在所有非抢占CPU调度算法中，系统平均响应时间最优的是（B）
A、实时调度算法
B、短任务优先算法
C、时间片轮转算法
D、先来先服务算法
分析：
短任务优先系统平均响应时间最短，但是往往不能确定所有任务的运行时间
先来先服务平均响应时间最长，不适用于分时系统
时间片轮转，适用于分时系统，但是增加了抢占以切换进程
算法性能依赖于时间片大小
####  3. 分配延迟：分配器停止一个进程到开启另一个进程的时间。
####  4. unix系统中,哪些可以用于进程间的通信。
（1）管道（Pipe）
（2）命名管道（named pipe）
（3）信号（Signal）
（4）消息（Message）队列
（5）共享内存
（6）内存映射（mapped memory）
（7）信号量（semaphore）
（8）套接口（Socket）